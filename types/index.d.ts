export declare class Example {
    constructor();
    init(content?: string): void;
    private toHump;
    initPage(tagName: string, innerText: string, styleConfig?: IStyleConfig): void;
}

export declare interface IStyleConfig {
    [key: string]: string;
}

export { }
